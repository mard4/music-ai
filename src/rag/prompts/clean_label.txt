Analyze this audio file to GENERATE A NEW SEMANTIC LABEL and REFINE ITS TAGS.

### AVAILABLE INFORMATION:
1. **ORIGINAL LABEL**: "{{label}}"
2. **ORIGINAL TAGS**: {{tags}}
3. **CONTEXT**: Music production sample library.

### YOUR TASK:
Perform a deep semantic analysis to produce two outputs: a descriptive **Caption** and a list of **Smart Tags**.

**PHASE 1: EXTRACTIVE ANALYSIS**
- Identify the **main sound category** (e.g., Kick, Bass, texture) from the label.
- If available, analyze the `ORIGINAL TAGS` to find technical details (e.g., "analog", "dry", "120bpm") and timbral qualities (e.g., "warm", "distorted").

**PHASE 2: CAPTION GENERATION**
- Create a concise, natural description.
- Structure: `[CATEGORY] + [TIMBRAL QUALITIES] + [TECHNICAL SPECS]`.
- Example: "Fat Analog Synth Bass with Warm Saturation".

**PHASE 3: SEMANTIC TAG REFINEMENT (The Filter)**
Review the `ORIGINAL TAGS` list and apply this logic to generate `smart_tags`:
1. **KEEP** tags that describe the sound texture (e.g., "warm", "fuzzy", "punchy").
2. **KEEP** tags that describe the instrument/category (e.g., "bass", "synth").
3. **DISCARD** contradictory tags (e.g., remove "drums" or "perc" if the sound is a "Bass").
4. **DISCARD** subjective/useless tags (e.g., "cool", "best", "new", "free").
5. **NORMALIZE** tags (e.g., convert "synths" to "synth").

### OUTPUT FORMAT:
Respond with a valid **JSON Object** containing exactly these keys:
{
  "caption": "The generated descriptive label",
  "smart_tags": ["tag1", "tag2", "tag3"]
}