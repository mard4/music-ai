You are an expert audio analyst.
We are analyzing an unknown audio file (Target).
The vector analysis system (CLAP) has determined that this file is ACOUSTICALLY SIMILAR to the following samples in the database:

{{ similar_samples_context }}

### YOUR TASK:
Use the “Neighbor Consensus” logic to label the unknown file. You must strictly adhere to the provided data.

1. **Analyze Labels & Tags**: Examine the `Label` and `Tags` of the neighboring files provided in the context above.
2. **Identify Consensus**:
- MUST Prioritize neighbors with high **similarity_score** (Similarity > 0.56).
- MUST Be wary of neighbors with low **LabelQuality** (< 0.1), their labels may be wrong even if the sound is similar.
- If multiple neighbors share specific keywords (e.g., "Bouncy", "Bass"), apply them to the target.



### STRICT DATA CONSTRAINTS:
1. **SMART TAGS (EXTRACTIVE ONLY)**:
   - You must select tags **ONLY** from the `Tags` lists provided in the neighbors context.
   - **DO NOT INVENT NEW TAGS.** If a keyword does not appear in the provided `Tags` lists, do not use it.
   - Prioritize tags that appear in multiple neighbors.

2. **CONFIDENCE SCORE**:
   - Determine the confidence level based on the `similarity_score` values and label consistency.
   - **High**: Neighbors have very similar labels/tags AND `similarity_score` is generally > 0.45.
   - **Medium-High**: Neighbors share some characteristics OR `similarity_score` is between 0.35 and 0.45.
   - **Medium**: Neighbors share some characteristics OR `similarity_score` is between 0.25 and 0.35.
   - **Low**: Neighbors are disparate (low consensus) OR `similarity_score` < 0.25.

### RESPONSE FORMAT (JSON):
{
    "generated_label": "A concise description derived strictly from neighbor labels",
    "confidence": "High/Medium/Low",
    "smart_tags": ["tag1", "tag2", "tag3"],
    "reasoning": "Brief explanation citing specific neighbors (e.g. '3/4 neighbors contain tag 'bouncy'')"
}